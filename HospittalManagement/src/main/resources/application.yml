spring.security.oauth2.client:
  registration:
    google:
      client-id: ${GOOGLE_CLIENT_ID}
      client-secret: ${GOOGLE_CLIENT_SECRET}
    github:
      client-id:  ${GITHUB_CLIENT_ID}
      client-secret: ${GITHUB_CLIENT_SECRET}
razorpay:
  key_id: rzp_test_TWhzzAm9iLrRzH
  key_secret: bplLSdH5242VIleddueOQrBI
  currency: INR

management:
  endpoints:
    web:
      exposure:
        include: "health,metrics,prometheus" # Ya "*"
  endpoint:
    prometheus:
      enabled: true

#YAML -> Yet Another markup language.